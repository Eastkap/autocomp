# Ledger (append-only audit log)

> Never edit or delete entries. Append only. One block per tick.
> Format: `## Tick N — <ISO date> — <stage>` then bullets for plan / dispatch / results /
> spend / approvals / learnings.

---

## Tick 0 — scaffold created
- Repo initialized; roles, tools, state, and the tick skill scaffolded.
- Stage set to `ideate` per `private/charter.md`.
- No spend. No outbound sends. No approvals requested.

---
